import { FETCH_LOGIN } from '../actions/types'

const initialState = { 
    login: []
}

export default (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}